---
galaxy_info:
  author: Pim van Helvoirt
  description: "role to configure fail2ban"

  license: GPL-3.0-only

  min_ansible_version: 2.1

  platforms:
    - name: Ubuntu
      versions:
        - jammy
    - name: Debian
      versions:
        - bullseye

  galaxy_tags:
    - fail2ban
    - nftables

dependencies:
  - role: pimvh.systemd_failmail
